{% extends 'themes/base_visitor.html.twig' %}

{% block title %}Nouveau Mot de Passe {% endblock %}

{% block description %}Nouveau Mot de Passe {% endblock %}

{% block keyword %}Nouveau, Password {% endblock %}



{% block main %}
    <h1 class="display-5 text-center my-3">Nouveau Mot de Passe </h1>


<div class="container  my-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            {{ form_start(resetForm) }}
                <div class="mb-3">
                    <label for="">Nouveau Mot de passe</label>
                    <div class="text-danger">{{form_errors(resetForm.plainPassword.first)}}</div>
                  {{ form_widget(resetForm.plainPassword.first, {'attr': {'class': 'form-control', 'autofocus': 'autofocus'} }) }}

                </div>

                <div class="mb-3">
                    <label for="Confirmation du nouveau Mot de Passe"></label>
                      <div class="text-danger">{{form_errors(resetForm.plainPassword.second)}}</div>
                    {{ form_widget(resetForm.plainPassword.second,{'attr':{'class':'form-control'}}) }}
                </div>

                 <div class="mb-3">
                    <button formnovalidate class="btn btn-primary">Réinitialiser</button>   
                </div>
            {{ form_end(resetForm) }}
        </div>
    </div>

</div>


{% endblock %}
